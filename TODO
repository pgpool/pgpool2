$Header$

TODO項目

* 全般
o SSLのサポート

o セッション開始時に発行する問い合わせをpgpool.confで設定可能にする

o 監査機能

o child は暇なときに accept しておく(LRU キューを作る)．

o クライアントが quit したら，LRUキューから次のクライアントを取り出す
  ->childがexitするとキューの中身が失われるが，それでよいか？

o DROP DATABASEのロジックを変更．今はparentにSIGUSR1を送ってすべての
  idleコネクションを切断しているが，DROP DATABASEしようとしているデー
  タベースへのコネクションを切断すれば十分．

* レプリケーション
o もしSQLをパースできれば，テーブル単位のレプリケーションも可能？

o セッションレベルのロードバランスを指示できる機能を追加
  /* BEGIN SESSION LEVEL LOAD BALANCE */とか．

o load balanceの比率をSQL文単位でコメントで指定できるようにする．

o OIDをreplicationする

o graceful shutdown, attach, detach の実装

o nextval や setval 以外にもレプリケーション対象とする関数を設定できる
  ようにする。


* クエリキャッシュ
o キャッシュの自動無効化の実装


* パラレルクエリ
o 複雑なクエリの高速化

o トランザクション処理の改良

o 拡張問合せプロトコル対応
